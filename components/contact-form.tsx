"use client";
import { useState } from "react";
import { ArrowRight, Check } from "lucide-react";
export function ContactForm(){const [state,setState]=useState<"idle"|"loading"|"success"|"error">("idle");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("loading");const data=Object.fromEntries(new FormData(e.currentTarget));const res=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});setState(res.ok?"success":"error")}if(state==="success")return <div className="form-success" role="status"><Check/><p className="eyebrow">Message received</p><h2>Thank you. I’ll be in touch.</h2><p>Your note is now in my inbox. I usually reply within two working days.</p></div>;return <form className="contact-form" onSubmit={submit}><div className="form-row"><label>Name<input name="name" autoComplete="name" required/></label><label>Work email<input name="email" type="email" autoComplete="email" required/></label></div><div className="form-row"><label>Company <span>(optional)</span><input name="company" autoComplete="organization"/></label><label>What would you like to discuss?<select name="interest" defaultValue=""><option value="" disabled>Select an area</option><option>AI strategy & feasibility</option><option>Technical product direction</option><option>Agentic systems & automation</option><option>Emerging technology</option><option>Speaking</option><option>Something less defined</option></select></label></div><label>What is difficult or uncertain right now?<textarea name="message" rows={7} required minLength={20}/></label><label className="check"><input type="checkbox" name="consent" value="yes" required/><span>I agree that Marc may use these details to respond to my inquiry. See the <a href="/privacy">privacy notice</a>.</span></label><input className="honeypot" name="website" tabIndex={-1} autoComplete="off" aria-hidden="true"/><button className="button" disabled={state==="loading"}>{state==="loading"?"Sending…":<>Send inquiry <ArrowRight size={18}/></>}</button>{state==="error"&&<p className="form-error" role="alert">The form could not be sent. Please email <a href="mailto:info@marcpaul.tech">info@marcpaul.tech</a>.</p>}</form>}
