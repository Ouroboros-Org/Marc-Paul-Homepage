import { notFound } from "next/navigation";import Link from "next/link";import { posts } from "@/lib/posts";
export function generateStaticParams(){return posts.map(p=>({slug:p.slug}))}
export default async function Post({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=posts.find(p=>p.slug===slug);if(!post)notFound();return <main id="main"><article className="article shell"><Link className="back" href="/thinking">← All thinking</Link><p className="post-meta">{post.date} · {post.read} read</p><h1>{post.title}</h1><p className="article-dek">{post.dek}</p><div className="article-body">{post.body.map(p=><p key={p}>{p}</p>)}</div><footer><p>Written by Marc Paul</p><a href="mailto:info@marcpaul.tech">Discuss this note ↗</a></footer></article></main>}
