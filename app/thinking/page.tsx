import Link from "next/link";import { ArrowUpRight } from "lucide-react";import { PageHero } from "@/components/ui";import { posts } from "@/lib/posts";
export default function Thinking(){return <main id="main"><PageHero label="Thinking" title="Notes on ambitious technology and the decisions around it."><p>Short essays on AI, agents, product systems and the useful tension between possibility and proof.</p></PageHero><section className="post-list shell">{posts.map((p,i)=><Link href={`/thinking/${p.slug}`} className="post-row" key={p.slug}><span className="mono">0{i+1}</span><div><p className="post-meta">{p.date} · {p.read} read</p><h2>{p.title}</h2><p>{p.dek}</p></div><ArrowUpRight/></Link>)}</section></main>}
